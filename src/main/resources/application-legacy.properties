# Legacy version specific configuration
info.app.version.type=legacy
info.app.description=Legacy version of StudyCICD application

# Custom metrics tags
management.metrics.tags.version=legacy
management.metrics.tags.deployment=legacy

# Logging configuration for legacy version
logging.level.junghun.studycicd=INFO
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss.SSS}|LEGACY|%thread|%logger{36}|%X{requestId:-}|%X{upstream:-}|%msg%n
logging.pattern.file=%d{yyyy-MM-dd HH:mm:ss.SSS}|LEGACY|%thread|%logger{36}|%X{requestId:-}|%X{upstream:-}|%msg%n

# Legacy specific log file
logging.file.name=/var/log/app/legacy-application.log